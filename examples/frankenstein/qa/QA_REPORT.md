# QA_REPORT · Frankenstein — The Hovel

- Date: 2026-07-30
- Round: **seventh**. Round 1 was the first independent QA (before it the example had no `qa/`
  stage at all). Round 2 drove the harness through a complete campaign to an ending. Round 3
  repaired the mouse input path that round 2 exposed. Round 4 made the `seen` ending render, and
  fixed a QA assertion of my own that could not hold at normal speed (F9). Round 5 made card text
  wrap inside its paper (F8) and gated it executably. Round 6 answered a human playtest report:
  the title's 4.5 s dead click window, the options plate's mouse trap, and the cold open's 23 s
  of held input with a frozen picture (F17/F18/F19, all **fixed**). Round 7 closed the last two
  open `major`s and found a third defect underneath one of them: it drew the holding plate §16.3
  had always specified and the build had never had (F15), it found why the suite could not go
  green at normal speed — not the missing `* SPEED` round 6 suspected but a QA stand point sitting
  exactly on an obstacle's push-out face (F16) — and while fixing that it found that a hotspot
  click had never performed its action when the creature started from a standstill (F20).
- Verdict: **not `PASS`** — but every automated gate now closes, **at both game speeds**. The
  slice is completable by either input scheme: full eight-night campaigns reach the `door` ending
  at exchange 5 and restart clean, one driven entirely by keyboard and one entirely by mouse, and
  all four designed endings (`door`, `seen`, `want`, `silence`) are played to their cards — the
  last two now at normal speed as well, which is what F16 was blocking. **All release gates
  close.** The verdict is still not `PASS` because what remains cannot be settled by a harness:
  clean-context onboarding comprehension and a human playtest both need a person, and the
  signature-frame pass needs re-judging by eye now that the yard it failed M3 and M9 on has been
  drawn (`qa/evidence/signature-frames.md`, *Re-judgement pending*). F4 and F13 stay open by
  decision, not by omission (see *Findings*, *Not verified*).
- `blocker`: 0 (1 found in round 1, **fixed**, with a regression check)
- `major`: 7, **all closed** — F1 (every release-gated asset: 6 plates, 2 Caslon faces, 12 audio
  cues), F6 (the dead mouse input path), F7 (the `seen` ending's unexported `wedge`), F12 (the
  hovel plate's composition), F15 (the holding plate), F16 (the suite at normal speed), F20 (the
  queued hotspot action)
- `minor`: 11 fixed, 3 open (F4 the cold open's authored 23 s hold, F13 the aperture reshape,
  F11's stated remainder)
- Deliverable name: **completable vertical slice, both input schemes, every release-gated asset
  in, both plates drawn, green at both speeds**

The automated result says this build boots, plays seven nights, takes the long walk, opens the
door, lands all five exchanges, reaches a designed ending, plays the epilogue and restarts to a
valid initial state — deterministically, under a fixed seed, at both game speeds. It does **not**
say the game is good, that the 45-minute arc lands, or that any human has played it.

## Environment

```bash
cd examples/frankenstein
node qa/design_invariants.mjs   # engine invariants, 10 sections
python3 qa/qa_browser.py        # real Chromium, eleven passes, accelerated
QA_SLOW=1 python3 qa/qa_browser.py              # normal speed; only this run's timing evidence counts
QA_ONLY=want,silence python3 qa/qa_browser.py    # named passes only, for iteration (writes -partial, never a gate)
```

| Item | Value |
|---|---|
| Page | `http://127.0.0.1:5199/?seed=42&fast=1` (and `?seed=42` under `QA_SLOW=1`) |
| Form | native ES Module + Canvas 2D, zero build |
| Viewport | 1280×800 (target), 1280×720 (minimum) both tested |
| Browser assertions | **333 passed, 0 failed** across eleven passes: keyboard, mouse, the keyboard campaign, the mouse campaign, all four designed endings (`door`, `seen`, `want`, `silence`), a card-layout gate over 2898 layouts, an epilogue text-size gate, a font gate proving both Caslon faces really render, and an audio gate driving all twelve cues from real state. The holding plate and the hovel plate are gated at pixel level inside the keyboard campaign |
| Engine invariants | all sections hold |
| Console errors / failed requests | 0 / 0 |
| Request domains | `127.0.0.1:5199` only — **no external domain** |
| Total build | 1.56 MB (6 plates as WebP + 2 woff2 at 33 KB; budget is 25 MB) |
| Frame p50 / p95 / worst | 8.3 / 10.0 / 10.4 ms — comfortably inside the 30 FPS floor (33.4 ms) |
| Main-thread long tasks | 0 in-game, 0 at boot |

Machine-readable summary: `qa/evidence/automated.json`. Frames: `qa/evidence/browser/` (51).
Full run log: `qa/evidence/qa_browser_last.log`.

Frame sampling sits on the two heaviest live screens (title reveal, night play), not an idle
screen, and reports the distribution rather than a mean.

**Pacing evidence, measured at normal speed on the finished build (2026-07-30):** frame
p50 8.3 / p95 9.6 / worst 10.4 ms against the 30 FPS floor of 33.4 ms; **no stall over 200 ms and no
main-thread block over 50 ms in game**. Both full campaigns complete eight nights to the `door`
ending at normal speed, and so do all four endings.

**The suite is now green at normal speed**, which is what F16 was blocking and what round 3's
claim about timing evidence was too broad about. `QA_SLOW=1` on the whole eleven-pass suite
returns **333 passed, 0 failed** with 0 console errors and 0 failed requests; the accelerated run
returns 333 passed, 0 failed. The committed `qa/evidence/automated.json` and frames come from the
normal-speed run, so the timing figures in this report and the evidence behind them are the same
run. `qa/evidence/qa_browser_last.log` holds the accelerated run for comparison.

The door scene runs on a real-time clock — 30 s a slot, and how many slots you get depends on the
family's store at dawn 6 (5, 3 or 2) — at either speed, so a full campaign takes about 6 minutes
accelerated and about 15 at normal speed. That is why one clean normal-speed suite takes roughly
an hour: `QA_ONLY` exists so a single pass can be re-run without paying for all of it.

## Findings and reflow

| # | Severity | Stage | Finding | Verification |
|---|---|---|---|---|
| B1 | `blocker` | `build` | **The title screen never completed.** `engine.tick()` owns `state.phaseTick`, and `step()`'s `title` case called `tickTitle(input)` without it — unlike `coldOpen` and `about`, which both call it. So `phaseTick` stayed 0 forever, `titleBeat` (= `phaseTick/45`) stayed 0, and the title text and all three verbs were never drawn. A player saw two blank shapes with no way in; only a blind Enter started the game | **Fixed** — `src/main.js` `case 'title'` now calls `engine.tick(state, {})`. Harness asserts `phaseTick >= 270` (full reveal); measured 414. Engine invariants still hold |
| F1 | `major` | `build` | All **6 release-gated image keys** and both Caslon fonts were absent; every one rendered as its grey box. Per `ART_DIRECTION` §16.1 a missing gated key means the build fails release | **Fully cleared — images, fonts and audio.** All 6 plates (`plate/paper`, `plate/title`, `plate/room`, `plate/hovel`, `plate/door`, `plate/fire`) generated, sized to their draw size and shipped as WebP (17 MB PNG → 1.4 MB, total build 1.50 MB against a 25 MB budget). They enter through `src/skin.js`; a key that fails to load still draws a grey box carrying its key name and the run continues. **Fonts now fixed too:** `font/caslon-text` and `font/caslon-display` ship as self-hosted woff2 Latin-1 subsets (16.6 / 15.7 KB, `OFL.txt` alongside), enter through `src/skin.js`'s key mechanism via `FontFace`, and are gated in the browser suite — `document.fonts.check` for both faces, neither key in `skin.pending()`, and canvas `measureText` width deltas proving each face really renders in place of Georgia and that Text and Display are distinct families. **Audio now done too:** all twelve §13.5 cues ship as procedural WebAudio. **Every release-gated asset is present** — 6 image keys, 2 Caslon faces, 12 audio cues. F1 is cleared |
| F2 | `minor` | `build` | Title line collided with the plate border: the plate ended at `PLATE.h-180` = 620 and the 30 px title sat at `0.78h` = 624, so the rule cut the letterforms. §3.3 puts occlusion tolerance at zero | **Fixed** — plate now stops at `0.72h`; title block clears the platemark |
| F3 | `minor` | `build` | In the hovel, the opaque prompt band (`0.88h`–`0.96h` = 704–768) was drawn over the tally plank (620–730) and completely hid the food heap (baseline 720). The plank carries Words and the heap carries own-food — both are read-outs the player is meant to check | **Fixed** — heap, plank and journal bundle raised to a `FLOOR` of 690 so they clear the band |
| F4 | `minor` | `design` | The cold open requires **23 s of continuously held input** before the first night, and `?fast=1` does not shorten it (`coldTime` counts real seconds, not night-minutes). It is a deliberate paced scene, but it is also 23 s before any interactive verb, and it makes every automated pass slow | **Open** — flagged to `game-world-design`; the duration stands because the pacing is an explicit authored beat. Round 6 cleared the defect underneath it (F19): the scene no longer sits frozen — every beat moves, the hold lights the chink, and a progress rule shows the hold landing |
| F5 | `minor` | `product` | `qa/` did not exist: no harness, no evidence, no report. `GAME_DESIGN` §14 named the acceptance path but nothing executed it | **Fixed** — `qa/qa_browser.py` added, following the repo convention (evidence in-workspace at `qa/evidence/browser/`, JPEG, never `/tmp`) |
| F6 | `major` | `build` | **Every mouse click was dead.** `src/main.js:66` declares `edges` with `action / drop / exit / journal / advance` and no `clicked`; line 104 does `const e = { ...edges }`, so `e.clicked` is permanently `undefined`, and line 106 clears `mouse.clicked` having never read it. Nine call sites depend on `input.clicked` — title buttons, click-to-move, exit-by-click, the knock, the door. All are unreachable. `mouse.down` is read as a *level* (lines 219 / 279 / 297), so held-mouse still drives the cold open and the context action, which is why round 1's "mouse-primary" pass went green: it exercised holds and silently fell through to Enter for everything else | **Fixed.** Three dead pieces, not one: (a) `consumeEdges` now reads `mouse.clicked` *before* clearing it, so one `mousedown` yields exactly one edge consumed by exactly one tick; (b) `scene.queuedAction` was declared, reset and read but **never assigned a value**, so `maybeQueuedAction` was a permanent no-op — a restored `clicked` alone would have walked the creature to a hotspot and left it standing there; (c) `yardClick` was defined at `main.js:167` and **never called**, its body carrying the no-op expression `engine.applyClick ? null : null;`. All three are wired, and the hotspot radii mirror `nearestActionable`'s per-object reaches exactly (DOOR 34, OUTHOUSE 40, WELL 30, DOOR_APRON 30, MILK_HOUSE 30, WOOD_EDGE 44, HOVEL_MOUTH 24). Verified by a **complete mouse-only campaign** to the `door` ending. The harness's silent `keyboard.press("Enter")` fallback — what let this hide in round 1 — is deleted |
| F7 | `major` | `build` | **The `seen` ending crashes the renderer.** `src/main.js:497` calls `R.wedge(...)`; `src/render.js:40` defines `wedge` as a module-local function and never exports it (the export list has no `wedge`). Once the `seen` phase begins, every frame throws `R.wedge is not a function` — the frozen cone and the failure card never draw and the console fills. The sim keeps ticking, so a keypress still advances to the epilogue over a dead screen. One of the four designed endings (§12) is unshippable | **Fixed** — `wedge` is now exported; it was the only one of the fifteen symbols `renderSeen` touches that `render.js` withheld. Verified by a dedicated `seen` pass: seed 42, night 1, the plank lifted through the real input path and the creature left standing in the yard until Agatha's retiring patrol finds him at minute 1.38 — no state injection. The pass asserts the sighting is the designed one (`ending=seen`, `seenBy=agatha`, `seenCtx=night`, inside the retiring window, seer's cone still live in the freeze), then the 2 s hold, the SEEN card, and the advance to the epilogue. Console-clean is asserted **segment-scoped** — zero *new* errors through the phase — on top of the untouched run-wide gate, so a run that never enters `seen` cannot pass it by default. Frames: `26_seen_freeze.jpg` (both whitened wedges, the creature inside Agatha's, the shadow across the seer), `27_seen_card.jpg` |
| F17 | `minor` | `build` | **The title's verbs took 4.5 s to appear, and clicks before they did were dead.** `titleBeat` was `phaseTick/45`, reaching the buttons' beat 6 only after 270 ticks, and `drawTitle` returns no hit boxes before that beat — so a first-time player clicking at the menu got nothing and read the mouse as broken (human playtest, round 6) | **Fixed** — the beat is 16 ticks (verbs up in ~1.6 s), a click before full reveal completes the reveal at once, Enter/Space activates the focused verb immediately, hover moves the focus underline (guarded so a parked mouse never fights the arrows), and returning from options/about restores the revealed title instead of replaying it |
| F18 | `minor` | `build` | **The options plate was a mouse trap.** Rows were card text, not targets: any click toggled the *focused* row, and focus moved on arrow keys only, so a mouse-only user could not even leave the plate except by reload — the harness carried a workaround comment saying exactly that | **Fixed** — every row is a real click target measured off the card's own layout (`layoutCard` records each wrapped line's logical source; the new `cardLineHits` turns that into row boxes, exposed to the harness as `__game.optionRows` so assertions stop hardcoding y positions); the last row is a mouse-reachable way back. Keyboard driving is unchanged — the audio pass's arrows+Enter path still passes |
| F19 | `minor` | `build` | **The cold open played 23 s of held input on a frozen picture.** `renderColdOpen` drew one static hovel view for the whole hold — the §10 beats (0:06 guitar, 0:14 Felix under the load, 0:18 taper out and pull-back, 0:22 the plank lifts) existed only as an empty `if (t < 18)` comment. The player heard the guitar, saw nothing move, and read the scene as stuck (human playtest, round 6) | **Fixed** — the new `drawColdOpen` implements the beats off held seconds: the dim aperture that brightens under a held hand, the guitar joining the old man, Felix home with the load and the girl helping him off with it, the taper-out fade and pull-back into the wide interior, the loose plank lifting with the prompt changing to `lift the plank`, and a thin progress rule above the prompt band. Held Enter now counts as a hold. The 23 s duration itself is unchanged — that stays F4's design call |
| F20 | `major` | `build` | **A hotspot click walked the creature over and then did nothing — whenever he started from a standstill.** `GAME_DESIGN` §5 promises mouse-primary play with one context action, and `main.js` implements it as `yardClick` setting `scene.queuedAction` plus `maybeQueuedAction` firing the action on arrival. But `maybeQueuedAction`'s stale-queue guard reads `state.target`, and `state.target` is only assigned inside `engine.tick`, which runs *after* `maybeQueuedAction` in the same tick. So on the click's own tick the creature has not started moving and `state.target` is still null, the guard's `!moving && !state.target` is true, and the queue it was meant to protect is destroyed the instant it is created. The action then never fires on arrival. This survived F6 and round 3's all-mouse campaign by accident: `click_arrive` stops polling inside a 14 px ring while the creature is *still walking*, so at every click in `mouse_carry` `creature.moving` was already true and the guard did not fire. It only bites when the creature is standing still — which is the normal case for a player, and for `take_once` right after stepping out of the hovel | **Fixed** — the guard now also excludes the tick that carries `input.target` (the snapped hotspot point, non-null on exactly that one tick), so a queue can no longer be cancelled before the walk it belongs to begins. Verified from a standstill at both speeds: one click on the milk-house walks over and lands the take (`takes` 0 → 1 at (688,253)), one click on the hovel mouth walks home and slips inside. Instrumented before the fix, `__game.queuedAction` read `null` 120 ms after the click with the creature still 47 px out; after it, the action fires on arrival. `mouse_carry` is unaffected — its queues already survived — so the all-mouse campaign still passes unchanged |
| F16 | `major` | `qa` | **The `want` and `silence` passes do not survive normal speed.** `QA_SLOW=1` on the finished build returns **272 passed, 2 failed**. Both failures are the same shape: the pass reaches "the walk to the milk-house" with `phase=dawnRead, minute 14.1 of a 14-minute night, inHovel=True` — the creature never got outside and the night expired. **The game is not at fault:** at the same normal speed both full campaigns play eight nights to the `door` ending and restart cleanly, keyboard and mouse, and the `seen` pass is console-clean. Only the two newest driver scripts fail, and both were written and verified at `?fast=1` only. Visible in them are two `wait_cond(..., 5)` calls with no `* SPEED`, where sibling waits use `10 * SPEED` — a plausible cause, **not a proven one** | **Fixed — and the cause was not the one guessed above.** The two unscaled `wait_cond(..., 5)` calls were real but harmless; scaling them changes nothing. The failure is geometric, and it is in `take_once`, which is the only helper the two failing passes share. The take reach is 30 px around `MILK_HOUSE` (710,252) while the milk-house obstacle, inflated by the creature's 8 px push-out radius, occupies x 688-732 / y 230-272 — so the ground a taker may actually stand on is a crescent about 8 px wide, and the pass drove the arrows at (688,252) with an arrival radius of 7. No fixed radius hits that band at both speeds. At `?fast=1` the creature covers ~20 px per 70 ms poll and crosses the ring by luck; at normal speed it creeps east to exactly x=688, the inflated face, where push-out pins it — `drive_to` then computes `want={ArrowUp}` forever, the creature never leaves (688,264), and the night expires. Reproduced under `QA_ONLY=want QA_SLOW=1`, and instrumented: held ArrowRight moves it 630→688 in 1.5 s and then not at all for the next 4.5 s. `take_once` now clicks the milk-house hotspot instead, so arrival is the engine's business rather than a poll's, at either speed. That click did not work when first tried, which is how F20 below was found; with F20 fixed a single click walks over and takes, and the walk home is one click on the hovel mouth. Separately, `drive_to`'s per-axis dead zone now derives from the arrival radius (`dz = clamp(radius/2, 1.5, 6)`) so that `dz*sqrt(2) < radius` always: with the old fixed 6 px zone any radius under 8.49 had a band where no key is held and the controller parks. Radii of 12 and up are unchanged, so no passing leg moved |
| F15 | `major` | `art` | **The yard plate was never drawn.** §16.3 lists as Canvas 2D work "the entire holding plate (walls in section, three hatched ground types, trees in outline, the pool, the sty, the well, the woodpile, the path, the lane gate)". None of it exists: the yard ships as flat-filled rectangles and two polylines on a navy field, with no hatching of any kind — the placeholder geometry the build began from. §3.1's *plan* view is correct and is not the problem; the plan was simply never rendered. This matters beyond cosmetics because the yard is where the game is played — every carry, every cone evasion, the whole of M3 | **Fixed.** The holding plate is now drawn as §16.3 lists it. Walls in section: a ruled band with the hatch clipped between its two lines, roof cut away, thickness and hatch spacing scaling with the building so a 14 px well head reads as a section as well as the 180 px cottage. The three ground surfaces and no fourth: snow as blank ground with the mould's chain lines barely showing, the cleared path as fine stipple on both runs (apron to lane gate, apron to milk-house), thawed earth as broken horizontals that the free-thaw-day flag rules into parallel furrows in the same generator. Trees in outline as jittered radial polygons with trunk ticks — arcs left open read as loose spirals at this scale. The pool stays the only unhatched white ellipse, the sty keeps its curved wall, the woodpile shows log ends in two courses, and the lane gate gets its posts. §3.2's "can I interact" language is now carried: actionable objects (milk-house, well head, outhouse) take a closed contour plus a hatched cast contact shadow whose offset scales with the object, while scenery stays open outline with none. §4.4's two prohibitions are honoured as prohibitions — the cottage interior stays flat plate tone with only the ruled hearth square, and no mark was added to the open ground between the outhouse and the door. Ground and fixed structures are cached to an offscreen plate keyed on the four values they read, so the added hundreds of strokes cost one draw per distinct state rather than per frame; everything answering to live state (the pile courses, the load, the pig, the tools, the portmanteau) is still drawn over it every frame. **Gated two-sided, and the gate has failed on purpose:** engraved regions must clear 30 colours and 600 luminance variance — measured cottage wall 49/2199.7, wood edge 418/1193.6, milk-house 136/3500.1, woodpile 157/3404 — while the two spec-mandated flats must stay under 14 colours and 40 variance, measured 1/0.0 (cottage interior) and 8/0.7 (open ground), so hatching either one fails as loudly as flattening the rest. Sampled on real play at night 3 with the creature outside and zero live cones asserted, no state injection. Reverting `holdingGround` to the pre-F15 flat fills scores the cottage wall at 2 colours / 114.3 variance and the gate rejects it. **Remainder:** the stipple and the furrows are not pixel-gated, because they only exist from night 5 and the campaign has the creature indoors on nights 5-7; they are carried by the signature-frame pass instead |
| F14 | `minor` | `qa` | **The position-filter assertion is weaker than it looks.** `[audio]` asserts the yard hearing is more open than the hovel's by comparing broadband RMS, and it passes — 0.02186 vs 0.02139. But that is a **2.2% margin**, because the hovel combines a 900 Hz low-pass (attenuating) with a reflection gain of 0.16 against the yard's 0.07 (adding). The two effects nearly cancel, so the test measures their net rather than the filter, and a modest change to either reflection gain could flip its sign | **Fixed** — the gate now compares energy **above 900 Hz** rather than broadband RMS: yard 0.01127 vs hovel 0.00425, a 2.65x margin against the old 2.2%. Fixed in the same round that added the other eleven cues, before they inherited the weak stage |
| F13 | `minor` | `art` | **The aperture is irregular but does not read as a chink in boards.** The rectangular clip is gone and the mask is now a `det()`-seeded sawtooth registered to the boards' own teeth, gated so a rectangle (variance 0) cannot pass. But the teeth are a uniform 7 px step on all four sides, so at game scale the aperture reads as a pinked or torn-paper mount, not §7.1's "ragged aperture — the chink in the boarded window". Prised boards give long, mostly straight edges broken at irregular points, not a constant-frequency fringe | **Open, deliberately deferred.** The machinery is sound and stays: `aperturePath` retraces the same tooth lines and salts as the four `plankBreak`/`plankEnd` calls, inset 2 px, so no room pixel escapes onto the boards and no sliver of plate shows between them — envelope bounds derived from the geometry (`by-2`, `lx+2`, `rx-2`), not tuned. Reshaping is held until **after** F12: regenerating `plate/hovel` to §7.1's composition changes where the aperture sits and what surrounds it, so cutting a new silhouette first would be thrown away. **Attempt 3 regressed** — stepped plank-ends now read as battlements on a flat navy panel, worse at game scale than the fringe they replaced; see `signature-frames.md`. Three attempts have failed the same way, treating the aperture as an outline to cut rather than board mass to lay over the room. **The regenerated plate now shows what the silhouette should be**, so the reshape has a concrete target rather than a prose description. It also left a small registration artifact: the plate's opening shows above the mask's top edge in a 52x11 px sliver (15 warm pixels at x 236-288, y 126-137), so the reshaped mask must cover the plate's opening completely |
| F12 | `major` | `art` | **The generated `plate/hovel` does not implement §7.1's composition, and the frame now carries two warm chinks.** §7.1 calls the hovel interior "the signature frame of this game" and puts a ragged aperture **left of centre**, "occupying about 34% of the frame's width and 40% of its height, warm, and inside it the whitewashed room". Measured on the shipped plate, its one warm opening sits at **x 868-886, y 308-502 — centred at x 877, right of centre, 1.4% of frame width and 24% of its height**: a light leak between boards, not an aperture onto a room. The game draws its own aperture at 160-590 (33.6% x 41.3%, left of centre) per §7.1, so the two do not register and the signature frame shows a warm rectangle left of centre *and* an unexplained warm crack right of centre. The plate's only large left-of-centre opening is cold and shows snow and trees, not the room | **Fixed.** `plate/hovel` regenerated via codex imagegen against §7.1, two variants produced and one rejected: the reject fragmented the wall into six or seven slivers where §7.1 asks for one aperture. The shipped plate measures **warm mass x 230-580, centred 405 — left of centre, with 0.0% of warm pixels right of centre** (was: centred 877, right of centre), and that mass sits inside the Canvas aperture at 160-590, so the room drawn over it covers it and one chink remains in frame. Its opening is edged as §7.1 describes — long straight board edges with splintered breaks across the grain — which also supplies the reference silhouette F13 was missing. **Consequence for QA:** the plate lights the straw, and the F11/F13 pixel probes were written against a dark-straw plate, keyed on "warm means content". Two broke — the aperture's bottom lane latched onto the plate's glow at y 500 and read it as a leak, and the plank control could not separate plank from lit straw (`#3a3020` passes `warm()` itself). Both corrected, not tuned: the bottom lane now measures the boundary from *inside* the room, and the control moved to the prompt band's straight edge, which is keyed on luminance and independent of the plate (scores 0 px^2 over 234 columns). Building those gates before regenerating the plate was the wrong order |
| F11 | `minor` | `art` | **Three objects were flat swatches on an engraved plate.** The cold slot, the tally plank body and the journal bundle were single `fillRect` fills with a 1.5 px stroke, sitting on top of the real `plate/hovel` woodcut like stickers. §7.1 asks the slot to be "a narrow cold slot where the loose plank sits, through which a strip of the yard is visible: the corner of the cottage and the pile of cut wood at the door"; §16.3 lists the pile courses, the tally plank and its scratches as Canvas 2D work | **Fixed, with a stated remainder.** A reusable `hatch` (density and broken lines, never a gradient), `plankBreak` and `plankEnd` for the boards' broken silhouette, and a deterministic index-hash `det()` for mark jitter — explicitly *not* the engine's seeded RNG, which would consume the sim's seed stream and desync replays. `drawPile` now takes a geometry argument and serves both the yard plan and the strip seen through the slot, so the pile courses cannot drift into two implementations. Gated at pixel level: each region must clear 40 unique colours and 150 luminance variance, with same-size bare-straw patches as controls that are themselves asserted, so a broken probe fails loudly. Measured slot 638/471, plank 145/1080, bundle 197/773 against a flat fill's ~3-6 colours and ~10 variance. **Remainder:** the slot's upper band between the cottage corner and the right end cap is still one dark tone with sparse marks and reads as a flat field at a glance; the plank's knots fall under the tally rows; the parcel is 50x40 px and reads more diagram than engraving. Carried to the signature-frame pass (queue 13), where "does it read" is the actual question |
| F10 | `minor` | `art` | **The card shrink ladder collides with `ART_DIRECTION` §9's own numbers.** Three parts. (a) §9's table gives card body 22 px with a floor "it must never cross" of 13 px, but `layoutCard` clamps at `Math.max(11, ...)` — below spec. Latent today (the worst observed setting is 16 px) but the clamp is wrong. (b) The ladder largely cancels the text-size setting on the busiest cards: the 4-line epilogue with buttons lands at 17 px / 18 px / 20 px for `textScale` 1 / 1.25 / 1.5, so a reader asking for 50% larger type gets 18%. (c) §9 also fixes a measure of ≤ 62 characters and specifies "left-aligned, ragged right"; the layout gate checks pixels only, and cards are drawn centred. The centring predates this round | **Fixed, code moved not the spec** — `ART_DIRECTION` §9 is untouched. (a) the body clamp is now §9's 13 px; (b) leading is 1.5, asserted in [1.45, 1.55] at every ladder rung; (c) the wrap is bounded by `min(innerW, 62 characters)`, so the measure no longer grows as type shrinks — it is asserted **in characters**, measured against Caslon rather than carried over from Georgia; (d) cards are left-aligned, ragged right. The alignment call was argued from §9's own reasoning: its *because* clause explains "never justified", not an exemption from "left-aligned", and centring destroys ragged-right's counterpart — the stable left return edge — which matters because the card population is not all short (the options card is 7 logical lines, long epilogue cards wrap to 5-8). Title plate, prompt band and door subtitles stay centred as single-line classes; card buttons stay centred with hit boxes unchanged, so the all-mouse campaign still passes. **The text-size setting is now proportionate**: capping the measure makes the wrap count size-invariant, so vertical demand grows linearly and the paper grows with the setting (716.8x320 -> 896x400 -> 1075x480, inside `CARD_LIMITS` clearing the moon arc and the prompt band). Heaviest options card 17 -> 23 -> 30 px against 21.3 / 25.5 requested; light cards exactly proportional. Gated over 2898 layouts: measure `[]`, leading `[]`, alignment `[]` |
| F9 | `minor` | `product` | **A QA assertion could not hold at normal speed.** `[mouse-campaign] click-to-move actually moves the creature` (added in round 3, by me) demanded >40 px of travel on a leg only 45.5 px long, while `click_arrive` stops polling inside a 14 px arrival radius — so the guaranteed floor is 31.5 px and the bar sat above it. The creature moves `WALK_SPEED` = 290 px per *night-minute*, i.e. 290 px/s at `?fast=1` but 36 px/s at normal speed, and the 100 ms poll therefore overshoots the arrival ring by ~29 px accelerated against ~3.6 px at normal speed. The assertion was a coin-flip at `fast` (observed both 43.6 px pass and 33.4 px fail) and unpassable under `QA_SLOW`. It aborted the mouse campaign, so **the round-3 claim that timing evidence was valid covered the keyboard campaign only** | **Fixed** — the bar is now derived from the geometry rather than hand-picked: travel must be at least the leg length minus the shared `ARRIVE_R` constant (which `click_arrive` also uses, so the two cannot drift). Standing still still fails it |
| F8 | `minor` | `build` | **The ending card's text overflows its plate and becomes unreadable.** `drawCard` (`src/render.js:348`) paints the card from `0.22w` to `0.78w` and then draws each line centred on `PLATE.w/2` with no wrapping and no fit-to-width. The epilogue's first line renders at 30 px display and is wider than the card, so it spills onto the `nightDeep` background still in `PAL.ink` — dark on dark. In `13_campaign_epilogue.jpg` the words "At da…" and "…sking." are effectively invisible. This is the payoff text of the whole slice | **Fixed** — `drawCard`'s inline geometry is lifted into a `CARD` constant; `wrapCardLine` does a `measureText` word walk in the style of the existing `glossText`; and `layoutCard` is a pure, exported measure that wraps every logical line to the paper's inner width and steps type and leading down a ladder until the block clears the paper's foot **and** the button zone. A word wider than the measure is left whole so the gate fails loudly rather than the card spilling ink. Button hit boxes are unchanged, so the all-mouse campaign still passes. Proved executably, not by eye: the harness walks the whole `STRINGS` tree and asserts width and height over **2898 layouts across 229 strings** — every card variant, with and without buttons, at `textScale` 1, 1.25 and 1.5. Overflow `[]`, misfit `[]`. Frame: `14_campaign_epilogue.jpg` |

## What was actually verified

- **A complete campaign to an ending.** Seed 42, played through the real input path with no state
  injection: listen at the chink on nights 1–2; night 3 carry a load to their door after the
  retiring window closes (Firing 0 → 1); night 4 the first lesson, then a second carry (1 → 2);
  lessons on nights 5–7; day 8 the long walk with all five slots, the yard crossed in daylight,
  the knock losing no slot; then the door, **all five exchanges landed** (96 words against the
  80-word gate, 2 carries against the exchange-5 lock), ending id `door`, the epilogue, `afterRun`,
  and a restart to a valid initial state. Reproduced at both speeds — `?fast=1` and `QA_SLOW=1`.
- **The same campaign again, driven entirely by mouse** — no keyboard at any point. Title verb
  clicked to enter; the about card and the options plate opened, toggled and dismissed by click;
  the chink clicked to listen; the cold slot clicked to step out; click-to-move across the yard
  with the creature's position asserted before and after each leg; the outhouse, the door pile and
  the hovel mouth clicked to take, put down and slip back in; four lessons started by click; the
  cottage door clicked to knock, keeping all five slots; five exchanges clicked; the withheld hand
  held with `mouse.down`; epilogue cards and restart clicked. Ending `door`, exchange 5.
- **Boot → title → cold open → night → night 2 → restart**, driven twice, once by keyboard only
  and once by mouse. `BUILD_BRIEF` requires every action to be reachable by keyboard alone; the
  keyboard-only pass completes the path.
- **Observable state moves** under input: `night`, `minute`, `words`, `store`, `unease` all change.
  A tick counter advancing was not accepted as evidence on its own.
- **Determinism**: same seed + same input sequence → identical state, twice.
- **Restart** returns night 1 / words 0 and the title phase.
- **Both viewports** (1280×800, 1280×720): no scroll overflow, canvas non-zero.
- **`prefers-reduced-motion`**: still renders.
- **Self-contained**: no external request domain, no failed requests.

Note on input: movement and the context action are **held**, not tapped — `main.js` reads
`keys.has(...)` every tick, so a press that releases inside one frame does nothing. The first
version of this harness tapped keys and wrongly concluded the simulation was frozen. Anyone
writing further automation against this build has to hold.

## Not verified this round

- ~~Endings~~ — **all four designed endings (§12) are now played end to end**: `door` and `seen`,
  plus `want` (five nights taking from the milk-house until `zeroStoreRun` reaches 3) and
  `silence` (the door clock run out at exchange index 0). Each asserts its id, a segment-scoped
  console-clean gate, `afterRun`, and a valid restart.
- **Endings by mouse other than `door`** — the mouse campaign reaches `door` only. `seen` is
  keyboard-driven; `want` and `silence` are keyboard-driven except the milk-house take and the
  walk home, which are hotspot clicks (that is what F16's fix made them). No pass reaches `want`
  or `silence` by mouse alone.
- **Signature frames**: the pass exists and judged the nine moments by eye
  (`qa/evidence/signature-frames.md`), but **M3 and M9 need re-judging**: they failed on the flat
  yard that F15 has since replaced, so their verdicts no longer describe the build. The cause is
  removed; the judgement has not been redone. M2 and M6 remain *not judged* — a transition cannot
  be assessed from stills.
- **Onboarding comprehension**: no clean-context judgement has been run
  (`qa/evidence/onboarding.md` does not exist), so first-time onboarding cannot be marked `PASS`.
- **The cleared path and the bed furrows are not pixel-gated.** Both surfaces only exist from
  night 5, and the campaign keeps the creature indoors on nights 5–7, so there is no frame in
  which the harness can sample them from real play. They are visible in the day-8 walk frame and
  carried by the signature-frame pass, not by an assertion.
- **Human playtest**: not started. `qa/PLAYTEST_PROTOCOL.md` is written and ready to run; no
  person has run it.

## Release gate

| Gate | State |
|---|---|
| 6 gated image keys | **present** — generated, sized, shipped as WebP, wired through `src/skin.js` |
| 2 Caslon fonts | **present** — Libre Caslon Text + Display, self-hosted woff2 Latin-1 subsets (16.6 / 15.7 KB) with `OFL.txt`, wired as `font/caslon-text` / `font/caslon-display` through `src/skin.js`; `document.fonts.check` true for both, `skin.pending()` holds neither, and canvas `measureText` proves both faces really render (not the Georgia fallback) and are not the same family |
| Release-gated audio set | **present** — all twelve §13.5 cues authored as procedural WebAudio (§13.4), no audio files. `audio.pending()` and `gatedPending()` both return empty. Each renders non-silent offline and silent with `sound=false`; the continuous beds are asserted as *state*, not triggers — `hearth/high` is 2.44x `hearth/small` (§13.2's second channel for Firing) and the wind's yard layer rises 4.6x for the last two night-minutes |
| 9 degradable keys | all currently on their named lesser expression |

**Every release gate is now closed** — 6 image keys, 2 Caslon faces, 12 audio cues — and the game
reads as the engraved book it was specified to be. That does not make this a release build, and
this report does not call it one: what is left is human judgement, not assets (see *Verdict* and
*Not verified*). The fallback path is intact and exercised: a key that fails to load draws a grey
box carrying its key name (or sets type in Georgia, for a font key) and the run continues.

**Integration debt still outstanding.** The Canvas state objects were positioned against the
greybox room; the room's hearth, board, stool and window have been re-anchored to fractions of the
aperture so they register on the real plate. The cold slot and the tally plank are no longer flat
swatches (F11) and the aperture is no longer a rectangle (F13's mask), but three pieces of greybox
geometry still sit over finished plates and want the engraved treatment:

- **The door scene's figures** (`drawDoorScene`): De Lacey is a disc and a rectangle, the
  firelight is an amber rect, and the "window onto the lane" is a flat `PAL.snow` rectangle laid
  over plate content that already reads as a wall. This is the game's climax frame, and the
  signature-frame pass called it `pass, with debt` for exactly this reason (M7).
- **The creature and his load**, everywhere: a filled disc, and a small amber bar when carrying.
  §5.2 accepts a silhouette in plan, but the carry has a read-out and no figure (M3).
- **The aperture's silhouette** still reads as a torn mount rather than prised boards — F13,
  deliberately deferred, with the regenerated plate now supplying the target shape.
