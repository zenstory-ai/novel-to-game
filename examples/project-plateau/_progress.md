# Project Plateau progress

- Source: Project Gutenberg eBook #139, exact local bytes recorded in `source/SOURCE.md`
- Mode: `quick`
- Current stage: `complete`
- Completed stages: `intake`, `analyze`, `concept`, `world-design`, `art-direction`, `build`, `qa`
- Selected concept: `Proof Before Dark` in `concepts/CONCEPT.md`
- Frozen build contract: `build/BUILD_BRIEF.md`
- Updated: 2026-08-03 PT
- Analysis coverage: `analysis/_coverage.md`; source 16 / success 16 / failed 0

`gate:intake pass` — all eleven product dimensions have values; user-confirmed 3D/English/genre intent and the bounded plan are separated from recorded implementation assumptions.

`gate:analyze pass` — the recorded Gutenberg bytes yield sixteen stable chapter boundaries; every body was read and compressed into `analysis/SOURCE_BIBLE.md`, with colonial-era framing retained as an explicit adaptation risk rather than treated as neutral world truth.

`gate:concept pass` — all three candidates preserve the locked first-person 3D survival-adventure frame, pass the hard vetoes and cite at least two verified high-adoption same-play precedents. Quick mode selects `Proof Before Dark` because one bounded run can test evidence quality, ecological response, limited defense and extraction together.

`gate:design pass` — `design/GAME_DESIGN.md` defines one core system, two supports, a five-beat complete run, first-minute copy, four deterministic outcome bands, a reachable numeric budget, a four-state decision flip and bound Strong/Mixed/Panic reference paths. It does not promote factions, combat breadth or a true open world into the prototype.

`gate:art pass` — `design/ART_DIRECTION.md` covers every designed space, unit, interaction and terminal state; binds all five core verbs to motion, sound and reduced-motion feedback; defines seven buildable signature moments; and separates release-gate assets from degradable fallbacks with explicit reference-rights boundaries. Real-render screenshot and video evidence remains a build/QA release gate rather than a claim made by this planning artifact.

`gate:build-brief pass` — `build/BUILD_BRIEF.md` freezes the approved loop before implementation, including exact first-minute copy, five verb-to-state contracts, the three-part action arc, ten release-gate asset keys, seven dynamic-media boundaries and one authoritative three-suite verification plan. Runtime results and final scope reconciliation remain open until the playable build exists.

`checkpoint:S0 pass` — the local Three.js `0.185.1` / Vite `8.2.0` foundation renders a real WebGL2 title, field-order handoff and representative 25,001-triangle glade at both locked viewports with no console errors or external request hosts. The recorded 240-frame headless sample measured 120.5 median FPS / 98.0 one-percent-low FPS, 874.4 ms observed navigation-to-ready and 138,574 gzip bytes. This checkpoint does not claim locomotion, collision, gameplay state, audio or a complete run; those remain open gates in `build/evidence/s0/report.json` and `build/asset-ledger.json`.

`checkpoint:S1 pass` — the deterministic controller now supplies mouse look, walk, sprint, crouch, foundation collision/slide, actionable boundary recovery, manual pause, focus-loss freeze and a clean restart. `build/evidence/s1/report.json` records the real Chrome path and a continuous 60.01-second / 30-input-segment smoke with zero console errors or external request hosts; its final 240-frame sample measured 120.5 median FPS / 109.9 one-percent-low FPS. Route topology, observation, threat, proof, defense, audio and extraction remain later gates.

`checkpoint:S2 pass` — the simulation now records a connected Fort → brook → canopy/basalt fork → glade → covered/exposed return history and drives the primary aerial silhouette through distant, watch, search and attack without a threat meter. A six-second covered hold de-escalates the state. `build/evidence/s2/report.json` binds all five real rendered states to hidden state snapshots, records zero console errors/external hosts and retains 120.5 median FPS / 101.0 one-percent-low FPS. Contact, defense, photographic proof, audio and extraction remain open.

`checkpoint:S3 pass` — examination now unlocks photographic context without awarding evidence; raising the camera slows movement, and a shutter release holds the live world for two seconds before spending one of four physical plates. Covered/open frames deterministically yield 0/1/2 cues and advance the hidden threat by the approved amount. One contact consumes the body margin and cracks the highest-value intact plate; a timely raised-rifle shot spends one of two cartridges, interrupts the dive and persists the gunshot flag. `build/evidence/s3/report.json` indexes seven independent state/browser/visual checkpoints, including pause during commitment, recoverable contact and defense, with zero console errors/external hosts and 120.5 median / 99.0 one-percent-low FPS. This remains a checkpoint rather than a complete run: deadline, route commitment, extraction, result/failure presentation and final audio are still open.

`checkpoint:S4 pass` — the primitive loop now spends the 420-second light budget, commits a 28-second covered or 12/18-second exposed return exactly once, submits only after the Fort gate and classifies intact evidence into the approved four alive bands. Timeout and a second unblocked contact show the exact cause plus one adjustable next-run cue; every terminal restart returns to the clean field order. `build/evidence/s4/report.json` indexes ten separate state/browser/visual checkpoints, including one continuous Strong-band proof → covered return → result → restart state path and both terminal failures. Its 240-frame sample measured 120.5 median / 97.1 one-percent-low FPS with no console errors or external hosts. The browser path compresses walking/time for checkpoint speed, so uncompressed Strong/Mixed/Panic references, exposed/noise route consequences, production assets/audio and the authoritative final handoff remain open.

`checkpoint:S5 pass` — all four alive evidence bands now have real browser output. A no-shot Strong path preserves six cues through the covered return; a Mixed path performs all five approved verbs, pauses a committed dive without advancing it, spends one cartridge, drives the brook brush response, pays the 18-second exposed/noise cost and returns a four-cue corroborating record with the exact gunshot callback. An attack-state 12-second creek return cracks the best plate without consuming body margin, while Panic reaches the explicit second-contact failure. `build/evidence/s5/report.json` binds eight state/browser/visual checkpoints plus a per-verb matrix, includes a `1280×720` Strong terminal, reports no console errors/external hosts and measures 120.5 median / 97.1 one-percent-low FPS. QA-only walking compression remains declared; uncompressed timing, production art/audio, final lifecycle/settings coverage and authoritative verification remain open.

`checkpoint:S6 pass` — the first-person field camera now has a wood rear standard, tapered bellows, ground glass, brass rails and lens; the aerial threat has separate membrane wings, body, head and crest; the covered route gains five physical branch arches and an attack-state pull-up visible above the canopy. Lazy local Web Audio supplies distinct observation, camera, shutter, plate, four-state threat, cover, rifle, contact, brook, result and failure cues without a remote asset request. Sound captions and independent ambience/effects/music controls are available before the run and survive a clean restart, while cue history resets per run. `build/evidence/s6/report.json` binds ten state/browser/visual checkpoints, reports no console errors/external hosts and measures 120.5 median / 98.0 one-percent-low FPS. The evidence proves browser audio state and cue scheduling, not subjective sound quality; final asset polish, persisted settings, uncompressed reference paths and authoritative handoff remain open.

`checkpoint:S7 pass` — presentation choices now round-trip through one validated versioned local record, tolerate malformed/blocked storage and offer a visible defaults reset. The browser matrix covers settings at 150% text, field order, first-person field, manual pause, focus-loss during a live shutter commitment, result and clean restart at the `1280×720` minimum, then repeats the heavy attack state at `1440×900`. Focus loss freezes the plate and suspends audio; restart keeps settings while resetting the run; reload returns to a clean title state. Under a no-cache 25 Mbps/20 ms Chrome profile, first frame arrived in 2,263.6 ms against the 8,000 ms budget. The production build totals 616,503 raw / 159,325 gzip bytes; heavy-state samples measured 120.5 median at both viewports, with 99.0 minimum / 100.0 target one-percent-low FPS. `build/evidence/s7/report.json` binds ten state/browser/visual checkpoints with no console errors or external hosts. Public-host cold loading remains open; independent non-colour review and uncompressed reference paths now pass.

`checkpoint:S8 pass` — real keyboard and mouse traversal now completes all three reference classes without invoking teleport or direct-time QA hooks. Strong performs all five verbs, records seven cues without firing, preserves its body margin and finishes the covered return in about 58.4 wall-clock seconds / 130.8 world units with about 93.5 seconds of light left; it deliberately breaks the dive under cover between the two behavior plates instead of relying on automation speed. Mixed records four cues, interrupts one dive, triggers the noisy-creek callback and returns with one cartridge; Panic spends both rounds and reaches the explicit second-contact failure. These measurements disproved the earlier 420-second / 5–8-minute paper estimate, so the approved documents and runtime now use a compact 1–3-minute loop with a 180-second light budget rather than padding the route with empty travel. The runner also repeats the complete Strong route and clean restart under Chromium achromatopsia. `build/evidence/s8/report.json` binds 21 state/browser/visual checkpoints, the full input trace and all four measured paths with no console errors or external hosts. Deterministic automation is not first-time human evidence: three external tester records and current public-host loading remain open.

`checkpoint:S9 pass` — the two-adult/three-young family now has readable neck, underside, hind/forelimb and thumb-spike structure plus separate graze, young-play and adult branch-pull roles. The two approved behavior shutters produce distinct `glade-young-play` and `glade-branch-pull` records and hold the corresponding family pose through the live commitment. Every shutter now captures the real renderer view with the physical camera hidden, keeps that exact local JPEG with its plate, presents it through a non-colour glass treatment and clears all images on restart; four browser-captured views had four distinct hashes. `build/evidence/s9/report.json` binds six state/browser/visual checkpoints, including both behavior commitments, both resulting plates, a four-view Strong result board and the clean order. It records 41 green unit tests, measured frame performance and no console errors or external hosts. S9 uses QA placement to isolate asset states, so S8 remains the traversal evidence; first-time tester records remain open.

`checkpoint:S10 pass` — the glade now reserves a 44-unit subject corridor, moves dense tree masses to the frame edges and places the family and red basalt in one warm lane with shadow and cool rim separation. Faceted animal planes, contrasting undersides and visible eyes improve the two-adult/three-young read without importing an external asset. The observation card now follows its approved 1.4-second hold, leaving the real gameplay frame free of explanatory prose before the player raises the camera. `build/evidence/s10/report.json` binds 18 state/browser/visual checkpoints: an unobstructed family sightline, both silver-frame behaviors, Chromium achromatopsia attack state, the four-view Strong board and clean restart, plus order/glade/attack-defense/result checkpoints under protanopia, deuteranopia and tritanopia. The protected focus region remains within its recorded pixel floors with no console errors or external hosts. Independent anatomy, composition, motion and colour-cue review now passes; first-time premise records remain open.

`media:core-loop pass` — `build/app/test/capture_demo_clip.py` records one continuous input-only Strong-result route at `1280×800`, including field order, traversal, four physical plate commitments, two limited defensive shots, an exposed return and the result. Exact 30-second and 15-second delivery versions use disclosed same-take cuts and measured per-segment speed changes so the camera, framed dive, first rifle response, branch proof, extraction and result remain readable. Both local MP4s pass 14/14 container, H.264 High Profile, `yuv420p`, scan, size, aspect, 30 FPS, duration, file-size, silent-track, non-black-first-frame and fast-start checks. The raw/derived videos stay out of Git; `build/media/clip/marks.json` and `manifest.json` retain their raw hash/bytes/duration, immutable interactive-source commit, edit map, every capture mark and full probe results. Separate ten-frame sheets cover the promoted 15-second and extended 30-second deliveries, while a crop-only 1280×640 share card remains reviewable in the repository. These edited delivery files do not replace S8 timing evidence.

`media:launch-video pass` — `build/media/remotion/` wraps the complete 30-second Strong delivery capture with a source-grounded English premise, five observable-action captions and a play/repository CTA. The historical 36-second V3 delivery passed its container, picture and mix checks, but that historical result does not approve a newly generated narration take. The current generator uses Fish Audio `s2.1-pro-free`, preserves short takes instead of stretching them, caps overruns at 30.8 seconds and writes an ignored schema-4 sidecar binding request, source, normalization and final hashes. `voiceover-review.json` remains `BLOCKED`; rights evidence and a hash-bound human listening record are required before `npm run render` will produce a new release candidate. Generated audio and delivery artifacts remain outside Git and understandable when muted.

`verification:authoritative pass` — the current `npm run verify` handoff ran 7/7 suites and 17/17 commands against source fingerprint `1b0ae821…`; exact timings live only in `qa/verification.json` and `qa/evidence/verify.log`. It includes unit/build coverage, S0–S10, input-only Strong/Mixed/Panic, design invariants, repository contracts, deterministic dual-viewport JPEG capture and hash-bound watch→bank→dive→pull-up motion evidence. `qa/verification.json` and `qa/evidence/verify.log` are the current structured evidence.

`qa:colour-vision-technical-matrix pass` — S8 supplies full-colour and achromatopsia complete input routes; S10 supplies order, glade, attack/defense and result checkpoints for protanopia, deuteranopia and tritanopia. The independent visual reviewer included the achromatopsia attack frame and passed light/colour and motion/feedback at the playable-prototype threshold.

`qa:independent-visual-review pass` — `/root/visual_verdict_round1` independently judged the approved targets, current stills and bound motion evidence without making implementation edits: 93/100, 0 blockers and 0 majors. All six prior visual minors are closed at the approved target finish. The durable rubric is `qa/evidence/independent-visual-review-2026-08-03.md`.

`release:playable-prototype evidence-ready` — current runtime QA, visual frames, independent review, focal assets and tested degradable fallback support `playable-prototype`. Public manifest/README promotion remains the publication lane’s responsibility.


`gate:build pass` — nine focal release assets have current authoritative runtime and independent visual evidence. Audio remains an explicitly classified degradable release asset with tested caption and visual/state fallback.

`gate:qa pass` — the 7/7 authoritative run and independent visual PASS support `demonstratedTier: playable-prototype`; all six prior visual findings are closed.

## Notes

`checkpoint:visual-promotion pass` — independent reviewer `/root/visual_verdict_round1` made no implementation edits and judged the approved targets plus current still and motion captures. The final verdict is PASS for `playable-prototype`: 93/100, 0 blockers and 0 majors. The complete rubric is `qa/evidence/independent-visual-review-2026-08-03.md`.

- The published reference project lives under `examples/project-plateau/`; promotion records a maintainer product decision, not a fabricated pass for evidence that was not retained.
- S0 technical work may establish renderer/input/performance feasibility in parallel, but it may not invent creature appearance, final topology, or encounter rules before concept/world/art ownership completes.
- No downstream stage may use modern *The Lost World* adaptation imagery or audio as source material.
