# 引擎选型参考：intake 阶段的生产引擎默认值锚点

> 用途：intake 给用户提"有信息量的引擎默认值"时的速查底本。合成自四块联网研究（网页/H5、客户端/移动、小程序/小游戏、品类专用+原型层）。**这是起点参考，不是结论**——落地前须复核（见文末）。

## 一、硬规则：引擎受什么约束 & 两层引擎必须分清

**约束轴**：引擎不是自由选的，由 **平台 × 画风 × 题材 × 市场** 共同夹逼。同一部小说，投主机大作、投国内小游戏、投出海 Web，答案完全不同——先定四轴，再点引擎。

**两层引擎（最容易混，务必讲清）**：

| 层 | 是什么 | 怎么选 | 本流水线关系 |
|---|---|---|---|
| **(a) 生产引擎** | 真做成品会用的引擎 | 按平台/画风/题材/市场，从下方主表点名 | intake 给"生产引擎默认值" |
| **(b) 原型层** | 本流水线的可玩交付切片 | **恒为"零构建网页可玩垂直切片"**（当前范例＝原生 ES Module + DOM/Canvas，浏览器直开） | 与选哪款生产引擎**解耦** |

**关键纪律**：原型切片默认 **不用** 生产引擎；选了 Unity/Unreal/Cocos 当生产引擎，也不代表原型要用它。intake 话术＝先按四轴点一个生产引擎默认值，再补一句"原型仍是零构建网页切片，不受该引擎绑定"。

## 二、主表：按平台选生产引擎（一页扫完）

| 平台 | 推荐生产引擎（2–3） | 2D/3D | 许可 / 商业要点 | 适合题材画风 |
|---|---|---|---|---|
| **客户端 PC / 主机** | **Unreal 5.7**；**Unity 6**（6.4/2026-03）；**Godot 4.x**（4.7/2026-06） | Unreal/Unity 3D 为主；Godot 2D 极强、3D 够用 | Unreal：终身营收 >$1M 抽 **5%**，Epic 商店销售免抽；Unity：**Runtime Fee 已取消**，Personal 免费（营收 <$200k），Pro ~$2k+/席·年；Godot：**MIT 零版税** | Unreal＝写实/电影感 AAA、高保真；Unity＝中量级 3D/二次元/卡通渲染/中重度手游；Godot＝像素/2D/独立/复古手绘 |
| **网页 / H5** | **Phaser 4**（4.1 Salusa）；**Three.js**/**Babylon.js 9**（3D）；**PlayCanvas**（小包体 3D） | Phaser 2D；Three/Babylon/PlayCanvas 3D | 全部 MIT；Babylon＝Apache-2.0（凭记忆）；PlayCanvas 引擎 MIT、云编辑器付费 | Phaser＝平台跳跃/射击/roguelike/像素/休闲；Three＝写实/低多边形探索/可视化；Babylon＝中重度 PBR/物理；PlayCanvas＝H5 互动广告/产品可视化 |
| **小程序 / 小游戏** | **Cocos Creator 3.8**（3.8.8，默认首选）；**LayaAir 3.3**（真 3D）；**Unity + 团结引擎**（重度复用） | Cocos 2D 强/3D 可；Laya **3D 强**；Unity 皆强 | Cocos/Laya 开源、免费可商用；Unity 有收入门槛条款、团结引擎另计 | Cocos＝2D 卡牌/放置/休闲/立绘/像素；Laya＝3D 动作/RPG/竖屏爽游；Unity＝重度 3D/已有 Unity 项目 |
| **移动 App（iOS/Android）** | **Unity 6**（画风不定时的安全默认）；**Godot 4.x**（独立/2D）；**Cocos 3.8**（超休闲） | Unity 皆强；Godot 2D 强；Cocos 2D+轻 3D | 同上各自条款 | Unity＝中重度手游/二次元；Godot＝像素/独立；Cocos＝超休闲/互动小游戏 |

**纯 2D 轻量补充**：**GameMaker**（PC/移动/HTML5，非商用免费、商用一次性约 $100）——像素/复古动作/Roguelite/街机风、快速出成品时轻便顺手；不做 3D/主机大作。

### 品类专用引擎（叙事 / VN / IF / JRPG，跨平台、多数 HTML 友好）

| 引擎 | 平台 | 2D/3D | 许可 / 商业 | 适合 |
|---|---|---|---|---|
| **Ren'Py 8.5** | 桌面/移动 + HTML5（WASM，beta） | 2D | MIT+捐赠，**商用免费** | galgame/视觉小说、立绘+CG；Web 导出体积大、移动端有限 |
| **TyranoScript / Builder** | 桌面/移动 + **HTML5 原生** | 2D | 免版税商用免费（Builder 需买 Steam 版） | VN/ADV，输出即 HTML5、**Web 亲和最好** |
| **Naninovel（Unity 插件）** | Unity 全平台含 WebGL | 2D(+3D) | **$150 永久** + Unity 许可 | 高制作值 VN、配音/Live2D/混玩法；重，原型不碰 |
| **RPG Maker MZ** | 桌面 + 浏览器 HTML5 | 2D | 需购买，**商用允许** | JRPG/探索/回合战斗/像素/中式古风；Web 产物臃肿 |
| **Twine 2.12** | **纯 HTML 输出** | 文字 | **GPL v3** 开源免费 | 互动小说/分支叙事/多结局/规则怪谈；天然贴合本流水线 |
| **Ink / inkjs** | 引擎无关，inkjs 跑 Web | 文字层 | **MIT 永久免费** | 叙事内核，嵌进任意前端/vanilla 切片 |

## 三、原型层速查：零构建网页切片怎么实现

**默认永远是手写 web 切片**（零构建、可控体积、可截图迭代）——但“手写零构建”约束的是
**怎么交付**，不是**做成什么**。切片形态跟着已选品类走，不倒过来让品类迁就最省的渲染方式：
玩法要玩家操纵空间与视角（走位、绕后、地形、镜头）就用 Three.js 做三维，要大量 2D 物体
与动作就用 Phaser，是卡牌 / 文字 / UI 重叙事才落到 Vanilla。**不得因为“二维更省”把一个
三维品类压成俯视图**——那正是把小说的文字形式套到玩法上的做法。选择只在下表内做：

| 实现 | 层 | 何时用 |
|---|---|---|
| **Vanilla Canvas/DOM** | 卡牌 / 文字 / UI 重叙事时的默认 | 文字/卡牌/像素/极简/UI 重叙事；首屏最小、无构建。**不是全品类默认**——选它要能说出玩法为什么不需要空间与视角 |
| **Phaser 4** | 原型（稍重） | 玩法动词＝动作/2D 多物体；生态最厚、文档最全 |
| **PixiJS v8** | 原型（纯渲染器） | 高粒子/华丽 2D/特效/视觉小说；**无内置玩法系统**，玩法自搭或配 Phaser |
| **Three.js** | 三维品类的默认 | 走位 / 绕后 / 地形 / 视角是玩法的一部分时选它：3D 探索、动作、ARPG。库随仓入零构建交付；相机与碰撞要自己搭，这是三维的**成本**，不是不做三维的**理由** |
| **Babylon.js 9** | 原型（3D 重） | 需物理/材质/编辑器的完整 3D 又要留在 Web |
| **生产引擎 Web 导出** | 可选，**非义务** | 仅当切片要验证的正是"成品观感"（见判据） |

**判据一句话**：
- 切片要证明的是 **"玩法"** → 手写 web（vanilla / Phaser / Three），与生产引擎解耦。
- 切片要证明的是 **"成品叙事观感"**（立绘调度/存读档/回放/密集分支）→ 才用能导出 Web 的品类引擎，**优先原生 HTML 系**（Twine 导 HTML、inkjs 挂脚本、Ren'Py/Tyrano 出 web build）。
- **明确禁用**：为了原型去强上 **Unity(Naninovel) / RPG Maker MZ** 的 Web 导出——产物几十 MB、非零构建、迭代慢，与"零构建垂直切片"冲突；这两者只写进"生产层建议"，原型仍用 vanilla/Phaser/Three 重做核心循环。
- Godot/Unity/Cocos/PlayCanvas/Babylon 均可导 Web build 做"更接近成品"的原型，但要接受包体、首屏、移动 web 的代价——**别把"原型必须用生产引擎"当默认**。3D 原型天然用浏览器内 Three.js，正好为将来上 LayaAir/Unity 的 3D 小游戏埋技术路线。

### 三维表现的路线锚点：实时 3D / 预渲染视频 / 混合

intake 给"3D"默认值时先把路线说清——三条路的选择边界是体验问题，不是库问题：

| 路线 | 适合 | 不适合 |
|---|---|---|
| **实时 3D**（Three.js / Babylon 原型层） | 视角或空间必须响应输入：自由走动、转视角、可旋转观察的物件 | 固定镜头叙事、纯氛围演出——用实时 3D 演固定镜头是自费性能 |
| **预渲染视频**（生成媒体产线或渲染导出） | 固定构图的过场、关键帧驱动的高潮演出、环境循环；画面临界、交互为零 | 任何需要输入改视角 / 改空间关系的场面 |
| **混合**（实时场景 + `VideoTexture` / 预渲染背景） | 3D 空间里嵌屏幕 / 全息影像，或实时角色叠在预渲染背景上 | 团队没有视频产线时别硬上——先把视频证据链建起来 |

两条硬要求随路线一起写进 intake 默认值，落地时由构建与 QA 出证据：

- **WebGPU 只作高端可选后端**：必须存在 WebGL2（或更低表现）fallback 并按探测切换，
  不能让高端机器看到另一个游戏；降级路径要求实测证据，不接受"应该能跑"。
- **资源释放要有证据**：geometry / material / texture / render target / video element /
  object URL / 监听器 / 动画循环都有属主与释放时机；场景重开、重复进入、后端降级
  各跑一遍回到基线，作为 3D 路线的验收下限。

## 四、小程序 / 小游戏专项提醒

**渠道引擎**：Cocos Creator 3.8（几乎全渠道，默认首选）、LayaAir 3.3（真 3D，国内 3D 小游戏市占最高）、Unity+团结引擎（重度 3D 复用，包体/启动成本最重）；**白鹭 Egret 约 2021 已停运，新项目不推荐**；渠道原生框架（微信 `wx.*` / 抖音 `tt.*` + Canvas）适合超轻 2D；Three.js+weapp-adapter 可把网页 3D 搬进微信，但 adapter 模拟不全需手工打补丁。

**包体 / 分包（已核实）**：微信主包 / 单个分包 ≤ **4MB**；开通虚拟支付整包 ≤ **30MB**，否则 ≤ **20MB**；抖音整包 ≤ **20MB**；超出资源走网络下载。**3D 素材极吃包体**，选 LayaAir/Unity 时分包与远程包是必修课。

**发布合规**：国内小游戏**必须备案**（软著 + 适龄提示 + 内容介绍）；开通虚拟支付/收费须**版号**，纯广告变现的免费游戏可暂免版号但仍需备案。

**成人内容禁令**：**成人 / 色情 / 露骨性暗示内容在国内所有小程序小游戏渠道一律禁止上线**（凭国内监管常识，本次搜索未取回渠道条款原文，已标注）。**若源小说属成人向，国内小游戏渠道此路不通**——只能改走出海 App/Web，或做合规化改编。这条直接呼应"NSFW 不能上小程序"。

## 五、起点参考，非结论：落地前务必复核

本文所有版本号、许可条款、平台能力都是**某一时点的联网快照 + 部分凭记忆判断**，会过期。**真要拿去当 intake 默认值或写进交接文档前**，先对拟采用的那一款做 WebSearch 复核：
- 引擎**当前版本**（是否已出新 LTS/大版本）；
- **许可与商业条款**（版税门槛、订阅价、Runtime Fee 类政策变动）；
- **平台能力**（WebGPU 转正与否、Web 导出限制、主机导出路径、小游戏渠道包体/合规规则）。

**联网不可用时**：不得把本文当权威，须在输出里显式标注 **"凭记忆·未核实"**，让下游知道这是待验证锚点而非定论。

## 六、核实情况

版本号与平台能力多为近期联网核实。以下仍属凭记忆或监管常识、落地前按第五节复核：
PixiJS/Babylon/Cocos 的 LICENSE 原文、成人内容渠道条款、GameMaker 主机档价格、Godot 主机
导出路径、抖音原生 `tt.*` 细节、Unity 团结引擎小游戏最新条款；表中首屏/包体多为经验判断
而非实测。